import React, {useEffect} from 'react'

export default function Entregar({setNavbarTitle}) {

    useEffect(() => {
        setNavbarTitle("Entrega");
    })

    return (
        <div>
            
        </div>
    )
}
