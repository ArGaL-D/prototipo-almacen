import React, {useEffect} from 'react'

export default function Prestamo({setNavbarTitle}) {
    useEffect(() => {
        setNavbarTitle("Préstamo");
    })

    return (
        <div>
            
        </div>
    )
}
