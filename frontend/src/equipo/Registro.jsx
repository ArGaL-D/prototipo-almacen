import React, {useEffect} from 'react'

export default function Registo({setNavbarTitle}) {

    useEffect(() => {
        setNavbarTitle("Registro");
    })

    return (
        <div>
            
        </div>
    )
}
