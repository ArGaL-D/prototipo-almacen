import React, {useEffect} from 'react'

export default function Usuario({setNavbarTitle}) {
    
    useEffect(() => {
        setNavbarTitle("Usuario");
    })

    return (
        <div>
            
        </div>
    )
}
